<li>
    <%= link_to user.username, user %>
    <% if current_user.admin? && !current_user?(user) %>
        <%= link_to 'delete', user, data: { "turbo-method": :delete,
                                            turbo_confirm: "Are you sure you want to delete #{user.username}?"} %>
    <% end %>
</li>