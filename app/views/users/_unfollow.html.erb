<%= form_with(model: current_user.active_relationships.find_by(followed: @user), html: { method: :delete }) do |f| %>
    <%= f.submit "Unfollow", class: "bg-red-500 text-white px-4 py-2 rounded shadow-md hover:bg-red-600 transition-colors duration-200" %>
<% end %>